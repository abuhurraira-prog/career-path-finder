---
import BaseHead from '../components/BaseHead.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import { SITE_TITLE, SITE_DESCRIPTION } from '../consts';
---

<!doctype html>
<html lang="en">
	<head>
		<BaseHead title={SITE_TITLE} description={SITE_DESCRIPTION} />
	</head>
	<body>
		<Header />
		<main>
			<section class="hero-container">
				<div class="hero-content">
					<h1 class="gradient-text">Navigate Your Future with AI</h1>
					<p class="hero-subtitle">
						Stop guessing. Start building. <strong>Career Path Finder</strong> uses intelligent analysis to map your CS skills to high-demand industry roles.
					</p>
					<div class="cta-group">
						<a href="/blog" class="btn-primary">View Project Logs</a>
						<a href="/about" class="btn-secondary">Learn the Tech</a>
					</div>
				</div>
			</section>

			<section class="features-grid">
				<div class="feature-card">
					<div class="icon">🤖</div>
					<h3>AI Analysis</h3>
					<p>Leveraging Python and Flask to analyze your technical proficiency and career interests.</p>
				</div>
				<div class="feature-card">
					<div class="icon">📊</div>
					<h3>Data Driven</h3>
					<p>Backed by a robust MySQL database of industry standards and job market requirements.</p>
				</div>
				<div class="feature-card">
					<div class="icon">🚀</div>
					<h3>Growth Roadmap</h3>
					<p>Receive a step-by-step personalized guide to transition from student to professional engineer.</p>
				</div>
			</section>
		</main>
		<Footer />

		<style>
			:root {
				--primary-gradient: linear-gradient(135deg, #6366f1 0%, #a855f7 100%);
			}

			main {
				width: 100%;
				max-width: 100%;
				padding: 0;
			}

			.hero-container {
				padding: 6rem 1rem;
				text-align: center;
				background: radial-gradient(circle at top, rgba(99, 102, 241, 0.1) 0%, transparent 70%);
			}

			.gradient-text {
				font-size: clamp(2.5rem, 8vw, 4rem);
				font-weight: 900;
				background: var(--primary-gradient);
				-webkit-background-clip: text;
				-webkit-text-fill-color: transparent;
				line-height: 1.1;
				margin-bottom: 1.5rem;
				letter-spacing: -0.02em;
			}

			.hero-subtitle {
				font-size: 1.25rem;
				color: #4b5563;
				max-width: 700px;
				margin: 0 auto 2.5rem;
				line-height: 1.6;
			}

			.cta-group {
				display: flex;
				gap: 1.2rem;
				justify-content: center;
				flex-wrap: wrap;
			}

			.btn-primary {
				background: var(--primary-gradient);
				color: white;
				padding: 1rem 2.5rem;
				border-radius: 50px;
				text-decoration: none;
				font-weight: 600;
				box-shadow: 0 10px 25px -5px rgba(99, 102, 241, 0.4);
				transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
			}

			.btn-primary:hover {
				transform: translateY(-3px);
				box-shadow: 0 20px 30px -10px rgba(99, 102, 241, 0.5);
			}

			.btn-secondary {
				background: white;
				border: 1px solid #e5e7eb;
				padding: 1rem 2.5rem;
				border-radius: 50px;
				text-decoration: none;
				color: #374151;
				font-weight: 600;
				transition: all 0.3s ease;
			}

			.btn-secondary:hover {
				background: #f9fafb;
				border-color: #6366f1;
				color: #6366f1;
			}

			.features-grid {
				display: grid;
				grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
				gap: 2rem;
				padding: 4rem 2rem;
				max-width: 1200px;
				margin: 0 auto;
			}

			.feature-card {
				padding: 3rem 2rem;
				background: #ffffff;
				border-radius: 32px;
				border: 1px solid #f3f4f6;
				text-align: center;
				transition: all 0.5s cubic-bezier(0.23, 1, 0.32, 1);
			}

			.feature-card:hover {
				transform: translateY(-12px) scale(1.02);
				border-color: #a855f7;
				box-shadow: 0 30px 60px -12px rgba(0, 0, 0, 0.1);
			}

			.icon {
				font-size: 3.5rem;
				margin-bottom: 1.5rem;
				display: block;
			}

			h3 {
				font-size: 1.5rem;
				margin-bottom: 1rem;
				color: #111827;
			}

			p {
				color: #6b7280;
				line-height: 1.7;
			}
		</style>
	</body>
</html>